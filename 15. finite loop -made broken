/* prog15.c: This programme loops 10,000 times with a byte.

 * Last Modified: 14/11/2012
 ***/
/* motivation to create finite loop
 * an alternative (xxxx) = print count**/
/* outputs not as expected: 
 * looped 0-255 repeatedly w/in 10,000 times**/

#include <stdio.h>
int main(void)

{
/*declare the byte; add count to monitor 10,000*/

	char byte = 0;
	int count = 0;

/*while loop indicates range -to 10,000*/

while (count < 10000)
{
/*loop body indicates means to end loop*/

	count = count+1;
	byte = byte+1;
	
	printf("%d\n", byte);
}	
	return 0;
}

/*create prog. w/a char variable
 *treat char variable as byte
 *initialise variable to have value of 0 (not '0')
 *create while loop -to loop 10000 times
 *in loop body, add 1 to char variable; printf new value in variable
 *run prog., look at output -does final printf output 10000, 9999 or else?  Why?
 ******/
