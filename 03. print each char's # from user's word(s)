//Prog 03
//27/02/2013 / 05/03/2013

//take user's input (any # of words), count each character's #, print in column

#include <iostream>
#include <string>
//#include <cstring>
#include <iomanip>
using namespace std;

int main()
{
	int ai=97,j;
	int charnum;
	string sentence;
	int counter=0;

	cout<<"Please, enter a short, random sentence.\n";
    getline (cin, sentence);
	cout<<sentence <<endl;
	charnum = sentence.size();
    cout<<"The number of characters is: " <<charnum <<"." <<endl;

//	int test;
    for(ai=97; ai < 123; ai++)
    {
        counter = 0;
//cout<<(char)ai <<endl;
        for(j=0; j < charnum; j++)
        {
            if((char)ai == sentence[j])
            counter++;
        }

    cout<<(char)ai << " = " << counter <<endl;
    }
    //cout<<"The number of instances of each letter is as follows:" <<endl;
    //cout<<'a = '<<(char)ai <<endl;
/*
	for(ai=0; ai<charnum; ai++) //checking for user entering capitals
		{
			if (islower(sentence[ai]==0))
				cout <<"You entered a capital, please, start again and enter sentence in lower-case letters." <<endl;
		}
	cout <<"The sentence you entered is: " <<sentence <<endl;

	cout <<"The number of characters in sentence is " <<charnum <<"." <<endl;

	do
	{
		for(ai=0; ai<charnum; ai++)

		switch(sentence[ai])
		{
		case 'a': a++; break;
		case 'b': b++; break;
		case 'c': c++; break;
		case 'd': d++; break;
		case 'e': e++; break;
		case 'f': f++; break;
		case 'g': g++; break;
		case 'h': h++; break;
		case 'i': i++; break;
		case 'j': j++; break;
		case 'k': k++; break;
		case 'l': l++; break;
		case 'm': m++; break;
		case 'n': n++; break;
		case 'o': o++; break;
		case 'p': p++; break;
		case 'q': q++; break;
		case 'r': r++; break;
		case 's': s++; break;
		case 't': t++; break;
		case 'u': u++; break;
		case 'v': v++; break;
		case 'w': w++; break;
		case 'x': x++; break;
		case 'y': y++; break;
		case 'z': z++; break;
		}
	}while (sentence[ai] != '\0');

	cout <<"The number of each letter is: " << a << b << c << endl;
	cout <<"Did the program get to here?";
*/
	return 0;
}
